#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string sent = get_string("Text: ");
    printf("%s \n", sent);
}

int count_letters(string text)
{
    int math = 0;
    return math;

}