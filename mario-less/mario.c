#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int blockHeight;

    printf("Enter a height number between 1 and 8: \n");
    scanf ("%d", &blockHeight);
    printf("Your number is: %d \n", blockHeight);

}